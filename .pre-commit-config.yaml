repos:
  - repo: local
    hooks:
      - id: unittest-fast
        name: retargeting unittest (fast)
        entry: bash -c 'uv run python -m unittest discover -s tests -p "test_*.py" -q'
        language: system
        pass_filenames: false
        always_run: true
